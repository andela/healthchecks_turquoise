machine:
  post:
    - pyenv global 2.7.12 3.5.2

dependencies:
  pre:
    - pip install -r requirements.txt
    - pip install mock

test:
  override:
    - python3 manage.py test
    - python2 manage.py test
